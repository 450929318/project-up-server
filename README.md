## 说明
```bash
1.通过node.js将本地前端项目静态文件包上传到指定服务器进行发布上线功能
2.需要先安装两个依赖
    npm install archiver@5.3.0   |   npm install node-ssh@12.0.2
```
## 开发

```bash
# 克隆项目
git clone https://github.com/450929318/project-up-server.git

# 安装依赖
npm install

# 启动服务
node index.js
```
